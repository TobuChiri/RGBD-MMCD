# CoLA Code Release

This release preserves the original workspace layout under:

- `DFormer/`
- `Sigma_CD/`

Datasets, logs, visualization dumps, temporary analysis outputs, CUDA installers, and bulk checkpoint folders are excluded. Best available checkpoints are copied back into their original relative paths; see `BEST_CHECKPOINTS.md`.

The original source workspace is not modified.
